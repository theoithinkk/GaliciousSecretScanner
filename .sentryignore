# SecretSentry's own ignore list, applied when you scan this repository with
# SecretSentry itself. Uses gitignore syntax.
#
# Everything below deliberately contains format-valid but fabricated secrets --
# they exist so the detectors have something to find. None of them authenticate
# against anything.

# Generates the vulnerable fixture repo; holds one planted secret per detector.
scripts/make_test_repo.py

# The test suite asserts on planted secrets throughout.
tests/
